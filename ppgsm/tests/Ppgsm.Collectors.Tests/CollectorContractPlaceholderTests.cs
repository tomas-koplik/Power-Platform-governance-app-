namespace Ppgsm.Collectors.Tests;

public sealed class CollectorContractPlaceholderTests
{
    [Fact(Skip = "Collector implementations and recorded API fixtures are owned by the Backend Collectors Engineer.")]
    public void Recorded_fixtures_satisfy_shared_contract()
    {
    }
}