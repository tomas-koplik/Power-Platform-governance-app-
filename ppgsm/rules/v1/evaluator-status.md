# V1 evaluator runtime status

## Runtime controls

| Control | Source confidence | Control intent | Data source | Required identity | Evaluator behavior | Severity and scoring | Remediation guidance | Open PoC question | Acceptance test |
|---|---|---|---|---|---|---|---|---|---|
| Catalog publication | Implemented contract | Prevent execution of unreviewed or incompatible rules | Trusted catalog and profile files | Service identity, local file read | Unknown key, evaluator-version mismatch, catalog-version mismatch, or attestation mismatch returns no publication and disables Score | No score is available | Correct and re-attest the catalog; no tenant write | None | Publication tests reject unknown keys and version 2 |
| Evidence gate | Catalog-declared; endpoint confidence remains collector-specific | Prevent conclusions from incomplete, lower-confidence, or absent paths | Completed section coverage plus immutable raw evidence references | Collector identity recorded per evidence reference | Requires every section, exact path, Full coverage, and declared confidence | NotEvaluated has no denominator effect | Re-run collection only after identity and endpoint prerequisites are met | T-01 through T-05 below | Acceptance partial-evidence fixture returns NotEvaluated |
| Coverage aggregation | Deterministic implementation | Report collection confidence without converting failed/skipped evidence to success | Required section coverage | None | Full only when all required sections are Full; Partial for mixed evaluable coverage; Failed/Skipped when no evidence is evaluable | Confidence only; no direct penalty | Investigate failed collectors or collect missing sections | None | Core theory covers Full, Partial, Failed, and Skipped |
| Zero denominator | Deterministic implementation | Avoid a numeric posture claim when no rule is evaluable | Persisted findings | None | Returns tier `Not evaluated`, score 0, and no area scores | No score claim | Resolve evidence/profile/PoC gates before interpreting posture | None | Core zero-denominator test |

## Evaluator registry

| Evaluator | Runtime status | Rule status in default profile | Evidence and applicability | Scoring effect | Remaining gate |
|---|---|---|---|---|---|
| `tenant.settingEquals` v1 | Enabled in registry | `PPG-TEN-006` advisory | Full documented `tenantSettings`; customer profile required | Excluded while advisory | Customer-approved provisioning profile and T-01 property stability |
| `dlp.coverage` v1 | Enabled in registry | `PPG-DLP-002` enabled | Full environments and DLP policy scope; measured environment ratio | Pass/Fail/Partial after PoC validation; ratio is measured, not fixed | T-02 tenant-wide DLP scope |
| `env.allProductionManaged` v1 | Enabled in registry | `PPG-ENV-002` disabled | Machine-readable production and protection level | Excluded while disabled | T-03 authoritative Managed Environment fields |
| `sharing.everyoneInDefault` v1 | Enabled in registry | `PPG-SHR-001` disabled | Full default environment plus app/flow assignments and customer sharing profile | Excluded while disabled | T-04 complete role assignments across identities |
| `resources.orphanCount` v1 | Enabled in registry | `PPG-SHR-002` advisory | Full app/flow owners and owner directory; unresolved lookup is NotEvaluated | Excluded while advisory | T-05 owner ID completeness and Graph outcome distinction |
| `tenant.settingIn` v1 | Enabled in registry | `PPG-AI-001` advisory | Full AI setting evidence and customer policy | Informational and zero score effect | Stable setting/documentation and customer Security/Privacy decision |

## Identity and scope

- PPAC Power Platform Administrator verification is persisted as a `TenantCapability` with tenant ID, principal object ID, source, and role. PPGSM `TenantMembership` remains a separate application authorization record.
- Snapshot jobs persist the user-requested environment filter. Snapshot completion separately persists discovered environment IDs and marks discovery authoritative only when the environments section is Full.
- Findings persist deterministic ID, rule version, catalog version, evaluator key/version, measured ratio, and exact raw evidence reference IDs. Raw settings remain immutable for reevaluation by newer catalogs.

## Auditable PoC gate matrix

No row below is an approval. An external approver must create a durable approval referencing captured evidence for the exact tenant, rule, identity, and API version. Approval expires at the recorded timestamp; mismatch, absence, or expiry leaves the rule `NotEvaluated` and has no scoring effect.

| Gate | Source confidence | Control intent | Data source | Required identity | Evaluator behavior | Severity and scoring effect | Remediation guidance | Open PoC question | Acceptance test |
|---|---|---|---|---|---|---|---|---|---|
| T-01 Tenant settings | Documented property intent; identity/version behavior is PoC-required | Establish stable tenant-setting evidence without inferring absent values | PPAC tenant settings response plus immutable request metadata | Delegated Power Platform Admin and Reader RBAC; each supported app-only identity separately | Only an exact unexpired tenant/rule/identity/API-version approval enables gated rules; otherwise `NotEvaluated` | Rule-defined severity only after approval; no penalty while gated | Recollect after permissions/version review; no tenant write | Are property names, null/absence semantics, and pagination stable for every supported tuple? | 200, 401/403, 404, 429, revocation, role-removal, and field mutation fixtures |
| T-02 DLP scope | DLP behavior documented; tenant-wide retrieval completeness is PoC-required | Prove all applicable policies and environments are represented | DLP policies, environments, scope selectors, continuation metadata | Delegated Admin, Reader RBAC, legacy app-only, and partial Environment Admin | `dlp.coverage` runs only with Full evidence and a matching approval; zero environments is `NotApplicable` | High when evaluable; Partial uses measured ratio; gated/non-applicable states do not score | Review policy scope and exclusions before any policy change; verify with a fresh snapshot; rollback may not reverse workload disruption | Does each identity/API version prove tenant-wide policy scope, and is partial visibility distinguishable? | Full, partial-access, pagination, exclusion, empty-tenant, and mismatch/expiry fixtures |
| T-03 Managed Environments | Product behavior documented; authoritative API fields are unproven | Distinguish production classification and managed state without name heuristics | Environment type and governance configuration fields | Same four identity scenarios, with license context recorded | Profile-disabled until approved; no production resources returns `NotApplicable` | Medium only when enabled and approved; otherwise excluded | Confirm licensing and owner impact; environment-level blast radius; refresh to verify; disabling may not undo prior governance effects | Which non-preview fields are authoritative and stable across identities/API versions? | Production managed/unmanaged, no-production, preview-field rejection, and identity mismatch fixtures |
| T-04 Sharing and assignments | Sharing semantics documented; assignment completeness is PoC-required | Detect broad principals only from complete app/flow role assignments | Default environment, apps, flows, role assignments, environment visibility | Delegated Admin, Reader RBAC, legacy app-only, partial Environment Admin | Runs only for customer-enabled profile and matching approved tuple; missing resources/profile decision is `NotApplicable` | Rule-defined severity; disabled/advisory/non-applicable does not score | Review owners and dependencies before narrowing access; resource/environment blast radius; snapshot verification; rollback may require restoring principals individually | Are broad principal types and all assignments returned across accessible and inaccessible environments? | Broad-principal, no-resource, partial-visibility, pagination, role-removal, and expiry fixtures |
| T-05 Ownership | Graph states documented; owner-ID completeness is PoC-required | Separate disabled/deleted owners from lookup or permission failure | Apps, flows, owner IDs, Graph user state and transport result | Power Platform read identity plus Graph `User.Read.All`; delegated/app-only tested separately | Disabled/NotFound may fail; unresolved/missing lookup remains `NotEvaluated`; exact approval required | High when evaluable; unresolved does not penalize | Review replacement owner and dependencies; resource-level blast radius; verify reassignment; rollback requires original owner availability | Are owner IDs complete, and are 404, disabled, throttled, and forbidden outcomes distinct? | Disabled, NotFound, Unresolved, 403, 429, missing owner ID, mismatch, and expiry fixtures |
| T-06 Tenant isolation and connectors | Documented product concepts; endpoint shape and completeness are PoC-required | Prevent posture claims from partial connector or isolation inventories | Tenant isolation policy, connector inventory/classification, pagination metadata | Delegated Admin, Reader RBAC, legacy app-only, partial Environment Admin | Collector remains disabled until evidence is approved for the exact tuple; evaluator returns `NotEvaluated` while gated and `NotApplicable` when profile/resource scope excludes it | No score before a published rule and approved evidence path | Security review required; tenant-wide/data-movement blast radius; verify both allow and deny paths; rollback limitations depend on connector usage | Which stable non-preview endpoints expose complete isolation and connector state for each identity? | Allow/deny, empty inventory, pagination, partial access, 401/403/404/429, revocation, and expiry fixtures |
| T-07 Publication provenance | Deterministic cryptographic implementation | Ensure evaluation and export use identical catalog/profile bytes and evaluator versions | SHA-256 manifest, catalog bytes, profile bytes, persisted finding provenance | Release identity for publication; runtime service identity for read | Byte mutation invalidates publication; export resolves exact approved digest or fails closed, never substituting current catalog | No score/export claim when publication is unavailable or mixed | Restore the retained reviewed publication or republish through external release approval; no governance write | Does retention preserve every digest referenced by findings for the contractual export period? | v1 findings after v2 current, catalog/profile/manifest mutation, untrusted digest, mixed finding set, and missing-retention tests |
| T-08 PoC approval lifecycle | Deterministic persistence; external approval evidence remains required | Prevent generic, stale, or fabricated PoC enablement | Durable approval plus immutable raw evidence reference | External approver identity recorded; runtime only reads approvals | Only exact tenant/rule/identity/API-version matches before expiry are supplied to evaluator | Approval itself has zero score; it only permits the rule's normal applicability/evidence evaluation | Re-run PoC and obtain independent approval; no automatic renewal; revocation requires expiry or an audited superseding lifecycle | What approval authority, maximum lifetime, and revocation SLA apply per customer? | Approve, exact match, tenant/rule/identity/version mismatch, evidence mismatch, expiry boundary, and no-approval tests |

Write remediation remains review-first. Any future execution path must state tenant/environment blast radius, identity and licensing prerequisites, post-change snapshot verification, and rollback limitations from the catalog before approval.